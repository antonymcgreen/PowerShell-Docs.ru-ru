---
ms.date: 05/17/2018
keywords: powershell,core
title: Известные проблемы в PowerShell 6.0
ms.openlocfilehash: ce40a1925e564fbd2c661e70ec36d3842d915dfe
ms.sourcegitcommit: 47becf2823ece251a7264db2387bb503cf3abaa9
ms.translationtype: HT
ms.contentlocale: ru-RU
ms.lasthandoff: 10/19/2018
ms.locfileid: "49451002"
---
# <a name="known-issues-for-powershell-60"></a><span data-ttu-id="538c7-103">Известные проблемы в PowerShell 6.0</span><span class="sxs-lookup"><span data-stu-id="538c7-103">Known Issues for PowerShell 6.0</span></span>

## <a name="known-issues-for-powershell-on-non-windows-platforms"></a><span data-ttu-id="538c7-104">Известные проблемы в PowerShell на платформах, отличных от Windows</span><span class="sxs-lookup"><span data-stu-id="538c7-104">Known Issues for PowerShell on Non-Windows Platforms</span></span>

<span data-ttu-id="538c7-105">Альфа-выпуски PowerShell в Linux и macOS по большей части функциональны, но имеют некоторые существенные ограничения и проблемы использования.</span><span class="sxs-lookup"><span data-stu-id="538c7-105">Alpha releases of PowerShell on Linux and macOS are mostly functional but do have some significant limitations and usability issues.</span></span> <span data-ttu-id="538c7-106">Бета-версии выпусков PowerShell в Linux и macOS более функциональны и стабильнее, чем альфа-версии, но в них все же может отсутствовать некоторый набор функций и могут содержаться ошибки.</span><span class="sxs-lookup"><span data-stu-id="538c7-106">Beta releases of PowerShell on Linux and macOS are more functional and stable than alpha releases, but still may be lacking some set of features, and can contain bugs.</span></span> <span data-ttu-id="538c7-107">В некоторых случаях эти проблемы — это просто ошибки, которые еще не исправлены.</span><span class="sxs-lookup"><span data-stu-id="538c7-107">In some cases, these issues are simply bugs that haven't been fixed yet.</span></span> <span data-ttu-id="538c7-108">В других случаях (как и с псевдонимами по умолчанию для ls, cp и т. д.) мы ожидаем обратной связи от сообщества относительно выбора, который следует сделать.</span><span class="sxs-lookup"><span data-stu-id="538c7-108">In other cases (as with the default aliases for ls, cp, etc.), we are looking for feedback from the community regarding the choices we make.</span></span>

<span data-ttu-id="538c7-109">Примечание. Из-за схожести многих базовых подсистем модуль PowerShell в Linux и macOS обычно имеет одинаковый уровень функций и ошибок.</span><span class="sxs-lookup"><span data-stu-id="538c7-109">Note: Due to the similarities of many underlying subsystems, PowerShell on Linux and macOS tend to share the same level of maturity in both features and bugs.</span></span> <span data-ttu-id="538c7-110">За исключением указанного ниже, проблемы, описанные в этом разделе, применяются к обеим операционным системам.</span><span class="sxs-lookup"><span data-stu-id="538c7-110">Except as noted below, the issues in this section will apply to both operating systems.</span></span>

### <a name="case-sensitivity-in-powershell"></a><span data-ttu-id="538c7-111">Учет регистра в PowerShell</span><span class="sxs-lookup"><span data-stu-id="538c7-111">Case-sensitivity in PowerShell</span></span>

<span data-ttu-id="538c7-112">Исторически сложилось так, что PowerShell был единообразно чувствителен к регистру, за некоторыми исключениями.</span><span class="sxs-lookup"><span data-stu-id="538c7-112">Historically, PowerShell has been uniformly case-insensitive, with few exceptions.</span></span> <span data-ttu-id="538c7-113">В операционных системах, подобных UNIX, файловая система преимущественно чувствительна к регистру, а PowerShell придерживается стандарта файловой системы. Это проявляется разными способами, очевидными и неочевидными.</span><span class="sxs-lookup"><span data-stu-id="538c7-113">On UNIX-like operating systems, the file system is predominantly case-sensitive and PowerShell adheres to the standard of the file system; this is exposed through a number of ways, obvious and non-obvious.</span></span>

#### <a name="directly"></a><span data-ttu-id="538c7-114">Непосредственно</span><span class="sxs-lookup"><span data-stu-id="538c7-114">Directly</span></span>

- <span data-ttu-id="538c7-115">При указании файла в PowerShell необходимо использовать правильный регистр.</span><span class="sxs-lookup"><span data-stu-id="538c7-115">When specifying a file in PowerShell, the correct case must be used.</span></span>

#### <a name="indirectly"></a><span data-ttu-id="538c7-116">Косвенно</span><span class="sxs-lookup"><span data-stu-id="538c7-116">Indirectly</span></span>

- <span data-ttu-id="538c7-117">Если скрипт пытается загрузить модуль, а имя модуля задано в неправильном регистре, то произойдет сбой загрузки модуля.</span><span class="sxs-lookup"><span data-stu-id="538c7-117">If a script tries to load a module and the module name is not cased correctly, then the module load will fail.</span></span> <span data-ttu-id="538c7-118">Это может вызвать проблемы с существующими скриптами, если имя, используемое для ссылки на модуль, не соответствует фактическому имени файла.</span><span class="sxs-lookup"><span data-stu-id="538c7-118">This may cause a problem with existing scripts if the name by which the module is referenced doesn't match the actual file name.</span></span>
- <span data-ttu-id="538c7-119">Завершение клавишей TAB не будет выполняться автоматически, если в имени файла неверный регистр.</span><span class="sxs-lookup"><span data-stu-id="538c7-119">Tab-completion will not auto-complete if the file name case is wrong.</span></span> <span data-ttu-id="538c7-120">Для завершения фрагмента необходим правильный регистр.</span><span class="sxs-lookup"><span data-stu-id="538c7-120">The fragment to complete must be cased properly.</span></span> <span data-ttu-id="538c7-121">(Для завершения имени и элемента типа регистр не учитывается.)</span><span class="sxs-lookup"><span data-stu-id="538c7-121">(Completion is case-insensitive for type name and type member completions.)</span></span>

### <a name="ps1-file-extensions"></a><span data-ttu-id="538c7-122">Расширения файлов .PS1</span><span class="sxs-lookup"><span data-stu-id="538c7-122">.PS1 File Extensions</span></span>

<span data-ttu-id="538c7-123">Скрипты PowerShell должны иметь расширение `.ps1`, чтобы интерпретатор мог определить, как загружать и запускать их в текущем процессе.</span><span class="sxs-lookup"><span data-stu-id="538c7-123">PowerShell scripts must end in `.ps1` for the interpreter to understand how to load and run them in the current process.</span></span> <span data-ttu-id="538c7-124">Выполнение скриптов в текущем процессе является ожидаемым обычным поведением для PowerShell.</span><span class="sxs-lookup"><span data-stu-id="538c7-124">Running scripts in the current process is the expected usual behavior for PowerShell.</span></span> <span data-ttu-id="538c7-125">К скрипту без расширения `.ps1` можно добавить магическое число `#!`, но в таком случае скрипт будет запущен в новом экземпляре PowerShell, из-за чего скрипт не будет работать должным образом при замене объектов.</span><span class="sxs-lookup"><span data-stu-id="538c7-125">The `#!` magic number may be added to a script that doesn't have a `.ps1` extension, but this will cause the script to be run in a new PowerShell instance preventing the script from working properly when interchanging objects.</span></span> <span data-ttu-id="538c7-126">(Примечание. Это может быть желательным поведением при выполнении скрипта PowerShell из `bash` или другой оболочки.)</span><span class="sxs-lookup"><span data-stu-id="538c7-126">(Note: this may be the desirable behavior when executing a PowerShell script from `bash` or another shell.)</span></span>

### <a name="missing-command-aliases"></a><span data-ttu-id="538c7-127">Отсутствуют псевдонимы команд</span><span class="sxs-lookup"><span data-stu-id="538c7-127">Missing command aliases</span></span>

<span data-ttu-id="538c7-128">В Linux и macOS были удалены "псевдонимы удобства" основных команд: `ls`, `cp`, `mv`, `rm`, `cat`, `man`, `mount`, `ps`.</span><span class="sxs-lookup"><span data-stu-id="538c7-128">On Linux/macOS, the "convenience aliases" for the basic commands `ls`, `cp`, `mv`, `rm`, `cat`, `man`, `mount`, `ps` have been removed.</span></span> <span data-ttu-id="538c7-129">В Windows PowerShell предоставляет набор псевдонимов, сопоставленных с именами команд Linux для удобства работы пользователя.</span><span class="sxs-lookup"><span data-stu-id="538c7-129">On Windows, PowerShell provides a set of aliases that map to Linux command names for user convenience.</span></span> <span data-ttu-id="538c7-130">Эти псевдонимы были удалены из версии PowerShell по умолчанию в дистрибутивах Linux и macOS. Теперь собственный исполняемый файл можно запускать без указания пути.</span><span class="sxs-lookup"><span data-stu-id="538c7-130">These aliases have been removed from the default PowerShell on Linux/macOS distributions, allowing the native executable to be run without specifying a path.</span></span>

<span data-ttu-id="538c7-131">Есть свои плюсы и минусы этого подхода.</span><span class="sxs-lookup"><span data-stu-id="538c7-131">There are pros and cons to doing this.</span></span> <span data-ttu-id="538c7-132">С удалением псевдонимов пользователь может использовать собственные команды PowerShell, но это снижает функциональность в оболочке, потому что собственные команды возвращают строки вместо объектов.</span><span class="sxs-lookup"><span data-stu-id="538c7-132">Removing the aliases exposes the native command experience to the PowerShell user, but reduces functionality in the shell because the native commands return strings instead of objects.</span></span>

> [!NOTE]
> <span data-ttu-id="538c7-133">Команда PowerShell ждет обратной связи в отношении этого аспекта.</span><span class="sxs-lookup"><span data-stu-id="538c7-133">This is an area where the PowerShell team is looking for feedback.</span></span>
> <span data-ttu-id="538c7-134">Какое предпочтительное решение?</span><span class="sxs-lookup"><span data-stu-id="538c7-134">What is the preferred solution?</span></span> <span data-ttu-id="538c7-135">Оставить все как есть или вернуть "псевдонимы удобства"?</span><span class="sxs-lookup"><span data-stu-id="538c7-135">Should we leave it as is or add the convenience aliases back?</span></span> <span data-ttu-id="538c7-136">Перейдите к [проблеме № 929](https://github.com/PowerShell/PowerShell/issues/929).</span><span class="sxs-lookup"><span data-stu-id="538c7-136">See [Issue #929](https://github.com/PowerShell/PowerShell/issues/929).</span></span>

### <a name="missing-wildcard-globbing-support"></a><span data-ttu-id="538c7-137">Отсутствует поддержка подстановочных знаков (подставки)</span><span class="sxs-lookup"><span data-stu-id="538c7-137">Missing Wildcard (globbing) Support</span></span>

<span data-ttu-id="538c7-138">В настоящее время PowerShell выполняет расширение подстановочных знаков (подстановку) только для встроенных командлетов в Windows, для внешних команд и двоичных файлов, а также командлетов в Linux.</span><span class="sxs-lookup"><span data-stu-id="538c7-138">Currently, PowerShell only does wildcard expansion (globbing) for built-in cmdlets on Windows, and for external commands or binaries as well as cmdlets on Linux.</span></span> <span data-ttu-id="538c7-139">Это означает, что команда, такая как `ls
*.txt`, завершится сбоем, потому что звездочка не будет расширена для соответствия именам файлов.</span><span class="sxs-lookup"><span data-stu-id="538c7-139">This means that a command like `ls
*.txt` will fail because the asterisk will not be expanded to match file names.</span></span> <span data-ttu-id="538c7-140">В качестве обходного решения можно выполнить `ls (gci *.txt | % name)` или, еще проще, `gci *.txt`, используя встроенный эквивалент PowerShell `ls`.</span><span class="sxs-lookup"><span data-stu-id="538c7-140">You can work around this by doing `ls (gci *.txt | % name)` or, more simply, `gci *.txt` using the PowerShell built-in equivalent to `ls`.</span></span>

<span data-ttu-id="538c7-141">Перейдите к [проблеме № 954](https://github.com/PowerShell/PowerShell/issues/954), чтобы дать нам обратную связь о том, как улучшить подстановку в Linux и macOS.</span><span class="sxs-lookup"><span data-stu-id="538c7-141">See [#954](https://github.com/PowerShell/PowerShell/issues/954) to give us feedback on how to improve the globbing experience on Linux/macOS.</span></span>

### <a name="net-framework-vs-net-core-framework"></a><span data-ttu-id="538c7-142">Платформы .NET Framework и .NET Core Framework</span><span class="sxs-lookup"><span data-stu-id="538c7-142">.NET Framework vs .NET Core Framework</span></span>

<span data-ttu-id="538c7-143">PowerShell в Linux и macOS использует .NET Core, которая является частью полной платформы .NET Framework в Microsoft Windows.</span><span class="sxs-lookup"><span data-stu-id="538c7-143">PowerShell on Linux/macOS uses .NET Core which is a subset of the full .NET Framework on Microsoft Windows.</span></span> <span data-ttu-id="538c7-144">Это важно, так как PowerShell предоставляет прямой доступ к базовым типам, методам платформы и т. д. В результате скрипты, которые запускаются в Windows, могут не работать в других системах из-за различий в платформах.</span><span class="sxs-lookup"><span data-stu-id="538c7-144">This is significant because PowerShell provides direct access to the underlying framework types, methods, etc. As a result, scripts that run on Windows may not run on non-Windows platforms because of the differences in the frameworks.</span></span> <span data-ttu-id="538c7-145">Дополнительные сведения о .NET Core Framework см. на странице <https://dotnetfoundation.org/net-core></span><span class="sxs-lookup"><span data-stu-id="538c7-145">For more information about .NET Core Framework, see <https://dotnetfoundation.org/net-core></span></span>

<span data-ttu-id="538c7-146">С выходом [.NET Standard2.0](https://blogs.msdn.microsoft.com/dotnet/2016/09/26/introducing-net-standard/) .NET Core 2.0 вернет многие традиционные типы и методы, представленные в полной версии .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="538c7-146">With the advent of [.NET Standard2.0](https://blogs.msdn.microsoft.com/dotnet/2016/09/26/introducing-net-standard/), .NET Core 2.0 will bring back many of the traditional types and methods present in the full .NET Framework.</span></span> <span data-ttu-id="538c7-147">Это означает, что PowerShell Core сможет загружать многие традиционные модули Windows PowerShell без изменений.</span><span class="sxs-lookup"><span data-stu-id="538c7-147">This means that PowerShell Core will be able to load many traditional Windows PowerShell modules without modification.</span></span> <span data-ttu-id="538c7-148">Вы можете следить за нашей работой, связанной с .NET Standard 2.0, [здесь](https://github.com/PowerShell/PowerShell/projects/4).</span><span class="sxs-lookup"><span data-stu-id="538c7-148">You can follow our .NET Standard 2.0 related work [here](https://github.com/PowerShell/PowerShell/projects/4).</span></span>

### <a name="redirection-issues"></a><span data-ttu-id="538c7-149">Проблемы с перенаправлением</span><span class="sxs-lookup"><span data-stu-id="538c7-149">Redirection Issues</span></span>

<span data-ttu-id="538c7-150">Перенаправление ввода не поддерживается в PowerShell на любой платформе.</span><span class="sxs-lookup"><span data-stu-id="538c7-150">Input redirection is not supported in PowerShell on any platform.</span></span>
[<span data-ttu-id="538c7-151">Проблема № 1629</span><span class="sxs-lookup"><span data-stu-id="538c7-151">Issue #1629</span></span>](https://github.com/PowerShell/PowerShell/issues/1629)

<span data-ttu-id="538c7-152">Используйте команду `Get-Content`, чтобы записать содержимое файла в конвейер.</span><span class="sxs-lookup"><span data-stu-id="538c7-152">Use `Get-Content` to write the contents of a file into the pipeline.</span></span>

<span data-ttu-id="538c7-153">Перенаправленный вывод будет содержать метку порядка байтов в Юникоде при использовании кодировки по умолчанию (UTF-8).</span><span class="sxs-lookup"><span data-stu-id="538c7-153">Redirected output will contain the Unicode byte order mark (BOM) when the default UTF-8 encoding is used.</span></span> <span data-ttu-id="538c7-154">Метка порядка байтов вызовет проблемы при работе со служебными программами, которые не ожидают ее, или при добавлении к файлу.</span><span class="sxs-lookup"><span data-stu-id="538c7-154">The BOM will cause problems when working with utilities that do not expect it or when appending to a file.</span></span> <span data-ttu-id="538c7-155">Используйте `-Encoding Ascii` для записи текста ASCII (который не является Юникодом и не будет иметь метку порядка байтов).</span><span class="sxs-lookup"><span data-stu-id="538c7-155">Use `-Encoding Ascii` to write ASCII text (which, not being Unicode, will not have a BOM).</span></span>

> [!Note]
> <span data-ttu-id="538c7-156">См. [RFC0020](https://github.com/PowerShell/PowerShell-RFC/issues/71), чтобы дать нам обратную связь по улучшению кодирования в PowerShell Core на всех платформах.</span><span class="sxs-lookup"><span data-stu-id="538c7-156">see [RFC0020](https://github.com/PowerShell/PowerShell-RFC/issues/71) to give us feedback on improving the encoding experience for PowerShell Core across all platforms.</span></span> <span data-ttu-id="538c7-157">Мы работаем над поддержкой UTF-8 без метки порядка байтов и будем менять значения кодировки по умолчанию для командлетов на разных платформах.</span><span class="sxs-lookup"><span data-stu-id="538c7-157">We are working to support UTF-8 without a BOM and potentially changing the encoding defaults for various cmdlets across platforms.</span></span>

### <a name="job-control"></a><span data-ttu-id="538c7-158">Управление заданиями</span><span class="sxs-lookup"><span data-stu-id="538c7-158">Job Control</span></span>

<span data-ttu-id="538c7-159">В PowerShell на Linux и macOS не поддерживается управление заданиями.</span><span class="sxs-lookup"><span data-stu-id="538c7-159">There is no job-control support in PowerShell on Linux/macOS.</span></span>
<span data-ttu-id="538c7-160">Команды `fg` и `bg` недоступны.</span><span class="sxs-lookup"><span data-stu-id="538c7-160">The `fg` and `bg` commands are not available.</span></span>

<span data-ttu-id="538c7-161">В настоящее время вы можете использовать [задания PowerShell](/powershell/module/microsoft.powershell.core/about/about_jobs), которые работают на всех платформах.</span><span class="sxs-lookup"><span data-stu-id="538c7-161">For the time being, you can use [PowerShell jobs](/powershell/module/microsoft.powershell.core/about/about_jobs) which do work across all platforms.</span></span>

### <a name="remoting-support"></a><span data-ttu-id="538c7-162">Поддержка удаленного взаимодействия</span><span class="sxs-lookup"><span data-stu-id="538c7-162">Remoting Support</span></span>

<span data-ttu-id="538c7-163">В настоящее время PowerShell Core поддерживает протокол удаленного взаимодействия PowerShell (PSRP) через WSMan с базовой проверкой подлинности в macOS и Linux и с аутентификацией на основе NTLM в Linux.</span><span class="sxs-lookup"><span data-stu-id="538c7-163">Currently, PowerShell Core supports PowerShell Remoting (PSRP) over WSMan with Basic authentication on macOS and Linux, and with NTLM-based authentication on Linux.</span></span> <span data-ttu-id="538c7-164">(Проверка подлинности на основе Kerberos не поддерживается.)</span><span class="sxs-lookup"><span data-stu-id="538c7-164">(Kerberos-based authentication is not supported.)</span></span>

<span data-ttu-id="538c7-165">Работа по удаленному взаимодействию на основе WSMan выполняется в репозитории [psl-omi-provider](https://github.com/PowerShell/psl-omi-provider).</span><span class="sxs-lookup"><span data-stu-id="538c7-165">The work for WSMan-based remoting is being done in the [psl-omi-provider](https://github.com/PowerShell/psl-omi-provider) repo.</span></span>

<span data-ttu-id="538c7-166">PowerShell Core также поддерживает PSRP через SSH на всех платформах (Windows, macOS и Linux).</span><span class="sxs-lookup"><span data-stu-id="538c7-166">PowerShell Core also supports PowerShell Remoting (PSRP) over SSH on all platforms (Windows, macOS, and Linux).</span></span> <span data-ttu-id="538c7-167">Хотя это в настоящее время не поддерживается в рабочей среде, вы можете узнать больше о настройке этой конфигурации [здесь](../core-powershell/ssh-remoting-in-powershell-core.md).</span><span class="sxs-lookup"><span data-stu-id="538c7-167">While this is not currently supported in production, you can learn more about setting this up [here](../core-powershell/ssh-remoting-in-powershell-core.md).</span></span>

### <a name="just-enough-administration-jea-support"></a><span data-ttu-id="538c7-168">Поддержка Just Enough Administration (JEA)</span><span class="sxs-lookup"><span data-stu-id="538c7-168">Just-Enough-Administration (JEA) Support</span></span>

<span data-ttu-id="538c7-169">Возможность создания удаленных конечных точек с ограниченными возможностями администрирования (JEA) в настоящее время недоступна в PowerShell в Linux и macOS.</span><span class="sxs-lookup"><span data-stu-id="538c7-169">The ability to create constrained administration (JEA) remoting endpoints is not currently available in PowerShell on Linux/macOS.</span></span> <span data-ttu-id="538c7-170">Эта функция не будет добавлена в 6.0. Мы рассмотрим ее добавление в последующих версия, так как для этого требуется значительная работа по проектированию.</span><span class="sxs-lookup"><span data-stu-id="538c7-170">This feature is currently not in scope for 6.0 and something we will consider post 6.0 as it requires significant design work.</span></span>

### <a name="sudo-exec-and-powershell"></a><span data-ttu-id="538c7-171">`sudo`, `exec` и PowerShell</span><span class="sxs-lookup"><span data-stu-id="538c7-171">`sudo`, `exec`, and PowerShell</span></span>

<span data-ttu-id="538c7-172">Так как PowerShell выполняет большинство команд в памяти (как Python или Ruby), вы не можете использовать sudo напрямую со встроенными модулями PowerShell. (Вы можете, конечно, запустить `pwsh` из sudo.) Если необходимо запустить командлет PowerShell с помощью sudo, например `sudo Set-Date 8/18/2016`, вам следует выполнить `sudo pwsh Set-Date 8/18/2016`.</span><span class="sxs-lookup"><span data-stu-id="538c7-172">Because PowerShell runs most commands in memory (like Python or Ruby), you can't use sudo directly with PowerShell built-ins. (You can, of course, run `pwsh` from sudo.) If it is necessary to run a PowerShell cmdlet from within PowerShell with sudo, for example, `sudo Set-Date 8/18/2016`, then you would do `sudo pwsh Set-Date 8/18/2016`.</span></span> <span data-ttu-id="538c7-173">Аналогично вы не можете напрямую выполнить встроенную команду PowerShell.</span><span class="sxs-lookup"><span data-stu-id="538c7-173">Likewise, you can't exec a PowerShell built-in directly.</span></span> <span data-ttu-id="538c7-174">Вместо этого вам нужно будет выполнить `exec pwsh item_to_exec`.</span><span class="sxs-lookup"><span data-stu-id="538c7-174">Instead you would have to do `exec pwsh item_to_exec`.</span></span>

<span data-ttu-id="538c7-175">Эта проблема сейчас отслеживается как часть проблемы [3232](https://github.com/PowerShell/PowerShell/issues/3232).</span><span class="sxs-lookup"><span data-stu-id="538c7-175">This issue is currently being tracked as part of [#3232](https://github.com/PowerShell/PowerShell/issues/3232).</span></span>

### <a name="missing-cmdlets"></a><span data-ttu-id="538c7-176">Отсутствующие командлеты</span><span class="sxs-lookup"><span data-stu-id="538c7-176">Missing Cmdlets</span></span>

<span data-ttu-id="538c7-177">Большое количество команд (командлетов), обычно доступных в PowerShell, недоступно в Linux и macOS.</span><span class="sxs-lookup"><span data-stu-id="538c7-177">A large number of the commands (cmdlets) normally available in PowerShell are not available on Linux/macOS.</span></span> <span data-ttu-id="538c7-178">Во многих случаях эти команды не нужны на этих платформах (например, функции Windows, такие как реестр).</span><span class="sxs-lookup"><span data-stu-id="538c7-178">In many cases, these commands make no sense on these platforms (e.g. Windows-specific features like the registry).</span></span> <span data-ttu-id="538c7-179">Другие команды, такие как команды управления службами (Get, Start, Stop-Service) присутствуют, но не работают.</span><span class="sxs-lookup"><span data-stu-id="538c7-179">Other commands like the service control commands (Get/Start/Stop-Service) are present, but not functional.</span></span> <span data-ttu-id="538c7-180">В последующих выпусках эти проблемы будут устранены. Неработающие командлеты будут исправлены и с течением времени будут добавлены новые.</span><span class="sxs-lookup"><span data-stu-id="538c7-180">Future releases will correct these problems, fixing the broken cmdlets and adding new ones over time.</span></span>

### <a name="command-availability"></a><span data-ttu-id="538c7-181">Доступность команд</span><span class="sxs-lookup"><span data-stu-id="538c7-181">Command Availability</span></span>

<span data-ttu-id="538c7-182">В следующей таблице перечислены команды, которые, как известно, не работают в PowerShell в Linux и macOS.</span><span class="sxs-lookup"><span data-stu-id="538c7-182">The following table lists commands that are known not to work in PowerShell on Linux/macOS.</span></span>

|<span data-ttu-id="538c7-183">Команды</span><span class="sxs-lookup"><span data-stu-id="538c7-183">Commands</span></span>|<span data-ttu-id="538c7-184">Рабочее состояние</span><span class="sxs-lookup"><span data-stu-id="538c7-184">Operational State</span></span>|<span data-ttu-id="538c7-185">Заметки</span><span class="sxs-lookup"><span data-stu-id="538c7-185">Notes</span></span>|
|--------|-----------------|-----|
|<span data-ttu-id="538c7-186">`Get-Service`, `New-Service`, `Restart-Service`, `Resume-Service`, `Set-Service`, `Start-Service`, `Stop-Service`, `Suspend-Service`</span><span class="sxs-lookup"><span data-stu-id="538c7-186">`Get-Service`, `New-Service`, `Restart-Service`, `Resume-Service`, `Set-Service`, `Start-Service`, `Stop-Service`, `Suspend-Service`</span></span>|<span data-ttu-id="538c7-187">Недоступно.</span><span class="sxs-lookup"><span data-stu-id="538c7-187">Not Available.</span></span>|<span data-ttu-id="538c7-188">Эти команды не распознаются.</span><span class="sxs-lookup"><span data-stu-id="538c7-188">These commands will not be recognized.</span></span> <span data-ttu-id="538c7-189">Это должно быть исправлено в последующем выпуске.</span><span class="sxs-lookup"><span data-stu-id="538c7-189">This should be fixed in a future release.</span></span>|
|<span data-ttu-id="538c7-190">`Get-Acl`, `Set-Acl`</span><span class="sxs-lookup"><span data-stu-id="538c7-190">`Get-Acl`, `Set-Acl`</span></span>|<span data-ttu-id="538c7-191">Недоступно.</span><span class="sxs-lookup"><span data-stu-id="538c7-191">Not available.</span></span>|<span data-ttu-id="538c7-192">Эти команды не распознаются.</span><span class="sxs-lookup"><span data-stu-id="538c7-192">These commands will not be recognized.</span></span> <span data-ttu-id="538c7-193">Это должно быть исправлено в последующем выпуске.</span><span class="sxs-lookup"><span data-stu-id="538c7-193">This should be fixed in a future release.</span></span>|
|<span data-ttu-id="538c7-194">`Get-AuthenticodeSignature`, `Set-AuthenticodeSignature`</span><span class="sxs-lookup"><span data-stu-id="538c7-194">`Get-AuthenticodeSignature`, `Set-AuthenticodeSignature`</span></span>|<span data-ttu-id="538c7-195">Недоступно.</span><span class="sxs-lookup"><span data-stu-id="538c7-195">Not available.</span></span>|<span data-ttu-id="538c7-196">Эти команды не распознаются.</span><span class="sxs-lookup"><span data-stu-id="538c7-196">These commands will not be recognized.</span></span> <span data-ttu-id="538c7-197">Это должно быть исправлено в последующем выпуске.</span><span class="sxs-lookup"><span data-stu-id="538c7-197">This should be fixed in a future release.</span></span>|
|`Wait-Process`|<span data-ttu-id="538c7-198">Доступна, но не работает должным образом.</span><span class="sxs-lookup"><span data-stu-id="538c7-198">Available, doesn't work properly.</span></span> |<span data-ttu-id="538c7-199">Например, "Start-Process gvim -PassThru</span><span class="sxs-lookup"><span data-stu-id="538c7-199">For example \`Start-Process gvim -PassThru</span></span> | <span data-ttu-id="538c7-200">Wait-Process" не работает. Происходит ошибка при ожидании процесса.</span><span class="sxs-lookup"><span data-stu-id="538c7-200">Wait-Process\` doesn't work; it fails to wait for the process.</span></span>|
|<span data-ttu-id="538c7-201">`Register-PSSessionConfiguration`, `Unregister-PSSessionConfiguration`, `Get-PSSessionConfiguration`</span><span class="sxs-lookup"><span data-stu-id="538c7-201">`Register-PSSessionConfiguration`, `Unregister-PSSessionConfiguration`, `Get-PSSessionConfiguration`</span></span>|<span data-ttu-id="538c7-202">Доступно, но не работает.</span><span class="sxs-lookup"><span data-stu-id="538c7-202">Available but doesn't work.</span></span>|<span data-ttu-id="538c7-203">Записывается сообщение об ошибке, указывающее, что команды не работают.</span><span class="sxs-lookup"><span data-stu-id="538c7-203">Writes an error message indicating that the commands are not working.</span></span> <span data-ttu-id="538c7-204">Это должно быть устранено в последующих выпусках.</span><span class="sxs-lookup"><span data-stu-id="538c7-204">These should be fixed in a future release.</span></span>|
|<span data-ttu-id="538c7-205">`Get-Event`, `New-Event`, `Register-EngineEvent`, `Register-WmiEvent`, `Remove-Event`, `Unregister-Event`</span><span class="sxs-lookup"><span data-stu-id="538c7-205">`Get-Event`, `New-Event`, `Register-EngineEvent`, `Register-WmiEvent`, `Remove-Event`, `Unregister-Event`</span></span>|<span data-ttu-id="538c7-206">Доступно, но нет источников событий.</span><span class="sxs-lookup"><span data-stu-id="538c7-206">Available but no event sources are available.</span></span>|<span data-ttu-id="538c7-207">Команды событий PowerShell присутствуют, но большинство источников событий, используемых с командами (например, System.Timers.Timer), недоступны в Linux, что делает команды бесполезными в альфа-выпуске.</span><span class="sxs-lookup"><span data-stu-id="538c7-207">The PowerShell eventing commands are present but most of the event sources used with the commands (such as System.Timers.Timer) are not available on Linux making the commands useless in the Alpha release.</span></span>|
|`Set-ExecutionPolicy`|<span data-ttu-id="538c7-208">Доступно, но не работает.</span><span class="sxs-lookup"><span data-stu-id="538c7-208">Available but doesn't work.</span></span>|<span data-ttu-id="538c7-209">Возвращается сообщение о том, что команда не поддерживается на этой платформе.</span><span class="sxs-lookup"><span data-stu-id="538c7-209">Returns a message saying not supported on this platform.</span></span> <span data-ttu-id="538c7-210">Политика выполнения — это ориентированный на пользователя "ремень безопасности", который помогает предотвратить дорогостоящие ошибки.</span><span class="sxs-lookup"><span data-stu-id="538c7-210">Execution policy is a user-focused "safety belt" that helps prevent the user from making expensive mistakes.</span></span> <span data-ttu-id="538c7-211">Она не является границей безопасности.</span><span class="sxs-lookup"><span data-stu-id="538c7-211">It is not a security boundary.</span></span>|
|<span data-ttu-id="538c7-212">`New-PSSessionOption`, `New-PSTransportOption`</span><span class="sxs-lookup"><span data-stu-id="538c7-212">`New-PSSessionOption`, `New-PSTransportOption`</span></span>|<span data-ttu-id="538c7-213">Доступно, но `New-PSSession` не работает.</span><span class="sxs-lookup"><span data-stu-id="538c7-213">Available but `New-PSSession` doesn't work.</span></span>|<span data-ttu-id="538c7-214">В настоящее время работа `New-PSSessionOption` и `New-PSTransportOption` не проверяется, так как работает командлет `New-PSSession`.</span><span class="sxs-lookup"><span data-stu-id="538c7-214">`New-PSSessionOption` and `New-PSTransportOption` are not currently verified to work now that `New-PSSession` works.</span></span>|
